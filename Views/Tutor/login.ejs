<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Tutor Login</title>

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Merriweather:wght@700&family=Roboto:wght@400;500&display=swap" rel="stylesheet">

    <style>
        body {
            font-family: 'Roboto', sans-serif;
         background: linear-gradient( #d15959, #e7abab, #d15959, #ce5b5b );
            min-height: 100vh;
            display: flex;
            flex-direction: column;
        }

        h1, h2, h3, h4 {
            font-family: 'Merriweather', serif;
        }

        .page-header {
            background-color: white;
            padding: 15px 30px;
            display: flex;
            align-items: center;
            justify-content: space-between;
            box-shadow: 0 2px 6px rgba(0,0,0,0.1);
        }

        .page-header img {
            height: 50px;
        }

        .btn-red {
            background-color: #b71c1c;;
            border-color: #b71c1c;
            color: white;
            text-transform: uppercase;
            font-weight: 500;
            letter-spacing: 0.5px;
            transition: transform 0.2s, box-shadow 0.2s;
        }

        .btn-red:hover {
            background-color: #8C1F1F;
            border-color: #8C1F1F;
            transform: scale(1.05);
            box-shadow: 0 5px 15px rgba(0,0,0,0.3);
            color: white;
        }

        .login-card {
            background-color: rgba(255, 255, 255, 0.9);
            padding: 40px;
            border-radius: 15px;
            box-shadow: 0 8px 20px rgba(0,0,0,0.2);
        }

        .footer {
            color: rgb(19, 18, 18);
            margin-top: auto;
            font-family: 'Merriweather', serif;
        }
    </style>
</head>
<body>

<header class="page-header">
    <h2 style="color: #e92121;">Tutor Portal</h2>
    <a href="/home">
        <img src="/assets/images/isu.png" alt="ISU Logo">
    </a>
</header>

<div class="container d-flex justify-content-center align-items-center flex-grow-1">
    <div class="login-card col-12 col-md-6 col-lg-4">
        <h1 class="mb-2 text-center" style="color: #e92121;">Login</h1>

        <% if (error) { %>
            <div class="alert alert-danger"><strong>Error:</strong> <%= error %></div>
        <% } %>

        <form action="/submitTutorLogin" method="POST">
        <input type="hidden" name="_csrf" value="<%= csrfToken %>">
            <div class="mb-3">
                <label for="tutorEmail" class="form-label">Email</label>
                <input id="tutorEmail" class="form-control" type="text" name="tutorEmail" required>
            </div>
            <div class="mb-3">
                <label for="tutorPassword" class="form-label">Password</label>
                <input id="tutorPassword" class="form-control" type="password" name="tutorPassword" required>
            </div>
            <button class="btn btn-red w-100" type="submit">Login</button>
        </form>
    </div>
</div>

<footer class="footer">
    <p>&copy; 2026 Illinois State University | Appointment System</p>
</footer>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>

</body>
</html>
