# AI Log

| Date | Tool Used | Prompt Summary | What Was Produced | What Changed Afterward |
|---|---|---|---|---|
| Feb 19, 2026 | GitHub Copilot Chat | Asked how to run the app locally | Identified Node.js/Express stack on port 3001; provided install and start commands; diagnosed and bypassed PowerShell execution-policy issue using cmd fallback | None |
| Feb 19, 2026 | GitHub Copilot Chat | Asked to create tutor folder/page similar to other login pages | Created tutor login page scaffold and linked it into project flow | Removed extra head/title markup to match minimal style used at that point |
| Feb 19, 2026 | GitHub Copilot Chat | Asked to add tutor routes to server | Added GET /tutorLogin and POST /submitTutorLogin routes; fixed admin file path spacing issue | None |
| Feb 19, 2026 | GitHub Copilot Chat | Asked how to create and push a branch (not main) | Provided and executed branch creation/push workflow commands | None |
| Feb 19, 2026 | GitHub Copilot Chat | Asked to rename branch from my-branch to Dillon | Renamed local branch to Dillon, pushed new remote branch, removed old remote branch | None |
| Feb 19, 2026 | GitHub Copilot Chat | Asked to merge Dillon into main and push | Guided/ran stage+commit, merge into main, and push steps | None |
| Feb 19, 2026 | GitHub Copilot Chat | Reported typo folder name tutuor should be tutor | Staged folder rename and pushed correction | None |
| Mar 9, 2026 | GitHub Copilot Chat | Asked to pull teammate branches and update local branch state | Fetched all remotes, created/tracked teammate branches, and synced main | None |
| Mar 9, 2026 | GitHub Copilot Chat | Reported Cannot GET / and asked for route/nav fixes | Added root redirect and /home route; updated cross-page navigation links to route-based URLs | None |
| Mar 9, 2026 | GitHub Copilot Chat | Asked to split tutor area into appointments/hours pages with matching style | Created tutorAppointments and tutorHours pages, added routes, and adjusted tutor sidebar/navigation styles | None |
| Mar 9, 2026 | GitHub Copilot Chat | Asked to improve tutor login page appearance | Reworked tutor login UI and added related style classes | None |
| Mar 9, 2026 | GitHub Copilot Chat | Provided assignment spec and asked for Mongoose/Auth implementation plan | Produced step-by-step implementation plan for DB models, auth, middleware, and validation | Used as planning reference before coding |
| Mar 9, 2026 | GitHub Copilot Chat | Asked to implement setup/model steps from plan | Installed dependencies; created env template/gitignore, DB connector, and core models; wired startup DB connect | Switched to Atlas URI and configured local .env accordingly |
| Mar 9, 2026 | GitHub Copilot Chat | Asked to add auth API routes and JWT middleware | Created register/login/me auth routes and JWT auth middleware | None |
| Mar 9, 2026 | GitHub Copilot Chat | Reported tutor login allowed wrong passwords; asked to enforce checks | Added bcrypt compare + DB user checks and startup seed user logic | Manually tested wrong-password and correct-password flows |
| Mar 9, 2026 | GitHub Copilot Chat | Asked to refactor Appointment comments shape | Changed comments storage to subdocument array schema | None |
| Mar 9, 2026 | GitHub Copilot Chat | Asked to push work to Dillon branch | Resolved git state and pushed branch updates to origin/Dillon | Conflict path handled before final push |
| Mar 9, 2026 | GitHub Copilot Chat | Asked to align current app to Stefan folder structure while preserving bcrypt/Atlas flow | Migrated active code/assets/views into Assets, Servers, Views; updated imports/routes/static paths; restored tutor/admin UI/nav text | Per-page smoke tests run; follow-up fixes applied for missing theme/text and placeholder nav labels |
| Mar 27, 2026 | GitHub Copilot Chat | Asked to rewrite README to accurately reflect the current Express+EJS codebase (correct routes, env vars, folder structure, tech stack) | Produced a fully rewritten README.md with accurate tech stack, route summary, required env vars, seeded test users, troubleshooting section, and project structure diagram | None |
| Mar 27, 2026 | GitHub Copilot Chat | Asked to run the application | Launched server via `npm.cmd start` in background terminal; confirmed MongoDB connected and app listening on port 3001 | None |
| Mar 27, 2026 | GitHub Copilot Chat | Asked to merge Dillon branch into main and push to origin | Ran `git merge Dillon`; resolved two conflicts (README.md kept rewritten version via `--ours`, studentController.js kept Dillon's newer version via `--theirs`); committed merge `7c20862` and pushed to `origin/main` | None |
| Mar 27, 2026 | GitHub Copilot Chat | Reported two startup errors after merge: duplicate AuditLog import and TypeError in studentRoutes.js | Removed duplicate `const AuditLog = require('../Model/AuditLog')` from `studentController.js`; replaced broken route handlers in `studentRoutes.js` (old: `createAppointment`, `studentCancelAppointment`) with correct modern handlers (`submitLogin`, `showDashboard`, `cancelAppointment`, `bookAppointment`) | App restarted successfully after each fix |
| Mar 27, 2026 | GitHub Copilot Chat | Asked to move bug fixes to Dillon branch without touching main | Stashed changes, switched to Dillon, applied stash, committed and pushed; resolved leftover stash conflict markers in `studentRoutes.js` (commit `3b9b061`) and pushed to `origin/Dillon` | None |
| Mar 27, 2026 | GitHub Copilot Chat | Asked to test the website after fixes | Ran full `Invoke-WebRequest` test suite against `localhost:3001`; all 12 routes returned expected status codes (public pages 200, auth API 200, protected dashboard redirects 200, wrong-password 401) | None |
| Mar 27, 2026 | GitHub Copilot Chat | Asked to merge main into Dillon and push | Ran `git merge main`; conflict in `studentRoutes.js` resolved by keeping Dillon's correct modern routes; committed `fe51ffd` and pushed to `origin/Dillon`; reran full test suite confirming all 12 routes still passed | None |
| Mar 27, 2026 | GitHub Copilot Chat | Asked whether teammates currently on main could pull Dillon without conflicts | Analyzed `git log` and `git diff main..Dillon --stat`; confirmed Dillon is 3 commits ahead of main with no divergent history (fast-forward eligible); advised teammates to `git fetch origin` then `git checkout Dillon` or `git merge origin/Dillon` | None |
| Mar 10, 2026 | GitHub Copilot Chat | Reported Mongo authentication failures for teammate setup | Diagnosed Atlas auth/network causes, generated teammate-ready URI patterns, and added optional URI selector approach before reverting to original env style | Kept original `MONGODB_URI` flow after user-requested rollback; provided final shared `.env` guidance |
| Mar 12, 2026 | GitHub Copilot Chat | Asked to verify if local branch was missing main updates and to run git commands | Fetched remotes, compared ahead/behind, fast-forward pulled `origin/main`, and pushed updated `Dillon` branch | Verified branch reached same commit as `origin/main`; reran app after dependency reinstall |
| Mar 16, 2026 | GitHub Copilot Chat | Asked to align admin/tutor login visuals with student and make tutor pages functional like admin scope | Restyled admin/tutor login pages; replaced static tutor placeholders with controller/routes/views for appointments + availability CRUD (no admin powers) | Added route-level checks and runtime validation; later upgraded to JWT cookie middleware and role-guarded tutor/admin routes |
| Mar 16, 2026 | GitHub Copilot Chat | Asked to harden auth and provide team demo-ready accounts | Implemented JWT cookie auth middleware and role authorization; seeded `testuser_admin`, `testuser_tutor`, `testuser_student`; validated login flows and protected route redirects | Updated logout behavior to clear cookie; confirmed seeded users authenticate successfully |
| Mar 17, 2026 | GitHub Copilot Chat | Asked for a full explanation of all files edited/created during the Mar 16 session to prepare for class presentation | Produced a detailed file-by-file breakdown of all 13 changed/created files, covering function-level logic, security patterns, and data flow for presentation use | No code changes; used as study and presentation reference material |
| Mar 17, 2026 | GitHub Copilot Chat | Asked for an exact list of which files were edited vs created in the Mar 16 commit | Listed 9 edited files and 4 newly created files with workspace-relative paths | None |
| Mar 17, 2026 | GitHub Copilot Chat | Asked for the local HTTP URL to paste into a Word doc for demo | Provided base URL `http://localhost:3001` and all key page route URLs (home, adminLogin, tutorLogin, dashboards) | None |
| Mar 17, 2026 | GitHub Copilot Chat | Asked where the code for AuditLogs, Users, and other stored data lives so professor questions could be answered | Mapped each data model file, the DB connector, and every controller that creates/reads/updates those collections; included plain-English presentation lines for each concept | None |
| Mar 17, 2026 | GitHub Copilot Chat | Asked to add student and tutor emails to AuditLog when admin cancels an appointment | Updated `cancelAppointment` in `adminController.js` to populate `student` and `tutor` fields and store both emails in `metadata` | None — verified by cancelling a new appointment and checking Atlas `auditlogs` collection |
| Mar 17, 2026 | GitHub Copilot Chat | Asked to add an Edit button next to Cancel on admin appointments table so admin can change appointment date/time | Added `editAppointment` controller method, `POST /admin/appointment/edit/:id` route, Edit button in admin dashboard table, Bootstrap-style modal with datetime-local inputs, and modal open/close JS | None — edit updates reflect immediately on tutor dashboard on next load |
| Mar 17, 2026 | GitHub Copilot Chat | Asked to remove the Cancel button from the tutor appointments view | Removed cancel form/button and Action column header from `Views/Tutor/dashboard.ejs`; tutor view is now read-only | None |
| Mar 17, 2026 | GitHub Copilot Chat | Reported duplicate Status column showing on tutor dashboard | Identified and removed the duplicate `<td>` status cell left over from the cancel-button removal | None |
| Mar 17, 2026 | GitHub Copilot Chat | Asked to add filter dropdowns (Date, Student, Course) above the appointments table on the admin dashboard | Added filter bar with three `<select>` dropdowns and a Clear button above the admin appointments table; implemented client-side JS sorting/filtering by date (newest/oldest), student name (A→Z/Z→A), and course; added supporting CSS | None |
| Mar 17, 2026 | GitHub Copilot Chat | Asked to push all session changes to main | Staged all modified files, committed with descriptive message including credit to Stefan, pushed `Dillon` branch to `origin/main` (commit `d616058`) | None |
| Mar 18, 2026 | GitHub Copilot Chat | Asked to check main and pull any new changes | Ran `git fetch origin`, switched to `main`, executed `git pull origin main`; confirmed fast-forward to commit `486a185` (Merge PR #9 stefan/admin-mvc); verified ahead/behind count 0/0 | None |
| Mar 18, 2026 | GitHub Copilot Chat | Asked to run the app | Diagnosed PowerShell execution-policy block on `npm.ps1`; switched to `npm.cmd start`; identified missing `node_modules` after pull, ran `npm.cmd install` (65 packages), restarted app; confirmed MongoDB connected and server listening on port 3001 | None |
| Mar 18, 2026 | GitHub Copilot Chat | Asked to stop the terminal | Terminated the background server process via `kill_terminal` | None |
| Mar 18, 2026 | GitHub Copilot Chat | Asked for MONGODB_URI format for teammate Rudy | Generated a ready-to-paste `MONGODB_URI` reusing existing cluster/DB/options from `.env`, swapping in Rudy's credentials; provided full three-line `.env` template | None — user copied URI directly into Rudy's `.env` |
| Mar 18, 2026 | GitHub Copilot Chat | Reported teammate Rudy getting "login failed" when signing in as admin | Reviewed `adminController.submitLogin`, `auth.js` middleware, `User` model, and seed logic; identified likely causes: wrong credentials, missing `active` flag, or unhashed password; added Rudy to seed list with force-reset logic targeting `rabramo@ilstu.edu`; narrowed root cause to missing `JWT_SECRET` in Rudy's `.env` | User reverted `server.js` seed additions after root cause confirmed; fix applied by adding `JWT_SECRET` to Rudy's `.env` |
| Mar 18, 2026 | GitHub Copilot Chat | Asked about automated booking confirmation emails for students and admins | Recommended Nodemailer + Gmail App Password; outlined four required steps: `npm install nodemailer`, `.env` vars (`EMAIL_USER`, `EMAIL_PASS`, `ADMIN_NOTIFY_EMAIL`), a reusable `Servers/mailer.js` utility, and a hook in `studentController.js` after appointment save | Not implemented this session — discussion and planning only |
| Mar 19, 2026 | GitHub Copilot Chat | Asked to run the application | Diagnosed PowerShell execution-policy block on `npm.ps1`; launched server with `node server.js` directly; confirmed MongoDB connected and app listening on port 3001 | None |
| Mar 19, 2026 | GitHub Copilot Chat | Asked to check main and pull any new changes | Fetched `origin/main`, identified 2 unpulled commits (`ce09025` added email service, `1230d92` Merge PR #10 stefan/admin-mvc); fast-forward pulled both; 4 files updated: `studentController.js`, new `Servers/middleware/emailService.js`, `package.json`, `package-lock.json` | None |
| Mar 19, 2026 | GitHub Copilot Chat | Asked whether nodemailer needed to be installed and to install it | Confirmed `nodemailer ^8.0.3` was added to `package.json` by the pulled commit; ran `npm.cmd install` to sync `node_modules` (1 package added); restarted server to verify clean boot | None |
| Mar 19, 2026 | GitHub Copilot Chat | Asked to add Gmail credentials to `.env` and restart the app | Added `GMAIL_ADMIN=itlearningcenterit@gmail.com` and `GMAIL_APP_PASSWORD` to root `.env`; killed old server process; restarted with updated env (dotenv loaded 5 vars); confirmed MongoDB connected and server on port 3001; booking confirmation email logged on first test booking | Updated `.env.example` to include `GMAIL_ADMIN` and `GMAIL_APP_PASSWORD` placeholder entries so teammates can replicate setup |
| Mar 19, 2026 | GitHub Copilot Chat | Asked to convert all 24-hour availability time displays to 12-hour format across the UI | Added `{ hour12: true }` to every `toLocaleString()` call in `Views/Admin/dashboard.ejs`, `Views/Home/home.ejs`, `Views/Tutor/dashboard.ejs`, and `Views/Student/studentLoginPage.ejs`; created and ran `scripts/migrateAvailabilityTimesTo12h.js` to reformat 5 existing AvailabilityBlock documents in MongoDB from HH:mm to h:mm AM/PM | None — migration script kept for documentation purposes |
| Mar 19, 2026 | GitHub Copilot Chat | Asked to audit codebase against project requirements rubric | Reviewed all four role sections (student, tutor, admin, general); identified missing student cancel endpoint, unwired cancellation emails, and absent admin reminder emails as the only gaps; all other requirements confirmed met | No code changes during audit — findings used to drive next implementation tasks |
| Mar 19, 2026 | GitHub Copilot Chat | Asked to add cancellation emails to admin and tutor cancel handlers | Imported `sendCancellationConfirmation` into `adminController.js` and `tutorController.js`; updated both `cancelAppointment` methods to populate `student` (name + email) and `tutor` (name) fields and fire the cancellation email after successful DB update | None — tested by cancelling an appointment and verifying email receipt |
| Mar 19, 2026 | GitHub Copilot Chat | Asked to add automated appointment reminder emails | Added `sendAppointmentReminder()` function to `Servers/middleware/emailService.js` (sends to both student and admin); added `sendUpcomingAppointmentReminders()` helper and `startReminderWorker()` scheduler (runs every 15 min) in `server.js`; used `NotificationLog` for deduplication so each appointment only gets one reminder email | Reminder worker fires immediately on startup; exported `sendAppointmentReminder` from emailService |
| Mar 19, 2026 | GitHub Copilot Chat | Asked to push email changes to main and Dillon branch | Staged and committed changes as "Add cancellation emails and scheduled appointment reminders" (commit `a866de4`); pushed to `origin/main` and `origin/Dillon`; restarted server to confirm reminder worker logs appeared | None |
| Mar 19, 2026 | GitHub Copilot Chat | Asked to pull origin/main and handle incoming Rudra PR #11 changes | Fast-forward pulled `origin/main` (commit `694863f` — Rudra's merged PR adding student home view); then reverted the merge commit via `git revert -m 1 694863f` (commit `d366632`) after user decided not to keep Rudra's version | Revert kept local only at time of session; not immediately pushed to origin |
| Mar 19, 2026 | GitHub Copilot Chat | Asked to pull Stefan's branch and merge his new student dashboard commit | Fetched `origin/stefan/admin-mvc`; identified new commit `0251b6f` ("added student dashboard, booking inside student page, and cancellation"); ran `git merge origin/stefan/admin-mvc`; 3 files updated: `studentController.js` (+171 lines), `studentRoutes.js` (+5 routes), new `Views/Student/dashboard.ejs` (225 lines) | Restarted server to pick up changes; pushed merge result to `origin/Dillon` (commit `f7f1661`) |
| Mar 19, 2026 | GitHub Copilot Chat | Asked to show who made an appointment and with whom in the database when a student books | Added `AuditLog` import to `studentController.js`; wired `AuditLog.create()` in `createAppointmentForSlot` (action `book_appointment`) and `cancelAppointment` (action `cancel_appointment`), storing `actor`, `targetType: 'Appointment'`, `targetId`, and metadata fields (`tutorId`, `course`, `startTime`, `endTime`) — the existing `Appointment` schema already stored `student` and `tutor` ObjectId refs and `createdAt` timestamp | Restarted server to confirm no startup errors; audit entries verified in MongoDB `auditlogs` collection |
| Mar 19, 2026 | GitHub Copilot Chat | Asked to push session changes to Dillon branch | Staged and committed audit-logging changes as "Add appointment audit logging to track who booked with whom" (commit `ebd7524`); checked out `Dillon` branch, pulled `origin/Dillon` (fast-forward, 17 files updated from remote), then pushed — remote already contained the commit so push reported up-to-date | None |
| Mar 20, 2026 | GitHub Copilot Chat | Asked for a real logout flow so pages don't appear logged-in after sign-out | Centralized `/logout` GET route in `server.js` (clears `auth_token` cookie, redirects to `/home`); removed duplicate logout from `tutorRoutes.js`; added global JWT-decoding middleware in `server.js` that always sets `res.locals.currentUser` from the cookie | None |
| Mar 20, 2026 | GitHub Copilot Chat | Reported header showed "Sign in as Admin" even when already logged in as admin | Rewrote `Views/Includes/_header.ejs` to be fully role-aware: reads `currentUser` from multiple fallback sources (`res.locals`, template `user` variable, `activeTab` context); shows Dashboard + Logout when authenticated, Sign-in link when guest; added `currentUser: req.user` pass-through in `adminController.showDashboard` | None |
| Mar 20, 2026 | GitHub Copilot Chat | Asked why adding a new tutor or student did not appear to save to the database | Traced POST `/addUser` — data was saving correctly; root cause was UX: success redirect defaulted to the Appointments tab so the new user was not visible; fixed redirects on success/error/duplicate to land on `?tab=users` with human-readable `notice` and `noticeType` query params | None |
| Mar 20, 2026 | GitHub Copilot Chat | Asked to verify appointments, auditLogs, availabilityBlocks, and notificationLogs all work and display readable names instead of ID strings | Audited all four collections end-to-end; added `buildAuditLogEntries()`, `summarizeAuditLog()`, `summarizeNotificationLog()`, `formatDateTime()`, `formatAppointmentLabel()`, `formatAvailabilityLabel()` helpers to `adminController.js`; enriched metadata on every `AuditLog.create()` call across all three controllers with `studentName`, `tutorName`, `course`; added `createUser`, `addAvailability`, `deleteAvailability` audit events that were missing; imported `NotificationLog` into `adminController.js` and loaded last 25 entries per tab load; added `recipientName` field to `NotificationLog` schema; rewrote all three email-send functions in `emailService.js` to use a `sendMailWithLog()` wrapper that creates a `NotificationLog` entry (with readable `summary`) on success and failure | Verified live end-to-end: availability add, student book, student cancel all produced readable log entries confirmed in rendered HTML |
| Mar 20, 2026 | GitHub Copilot Chat | Asked to add a new Activity Logs tab to the admin dashboard | Added `logs` tab button to sidebar in `Views/Admin/dashboard.ejs`; added full tab content section with two tables — Audit Log (columns: Time, Actor, Action, Target, Summary) and Notification Log (columns: Time, Event, Recipient, Status badge, Summary); `validTabs` list in `adminController.js` updated to include `'logs'` | None |
| Mar 20, 2026 | GitHub Copilot Chat | Asked to add named success/warning banners to the Appointments tab after admin cancel or edit | Updated `cancelAppointment` redirect to `?tab=appointments&noticeType=warning&notice=Cancelled [StudentName]'s appointment with [TutorName].`; updated `editAppointment` redirect to `?tab=appointments&noticeType=success&notice=Rescheduled [StudentName] with [TutorName].`; added notice banner block to top of appointments tab in `dashboard.ejs` matching the pattern already used on hours and users tabs | Verified live: cancel of Test Student / Hash Check Tutor produced correct named banner in rendered HTML |
| Mar 20, 2026 | GitHub Copilot Chat | Asked to add readable names directly to MongoDB documents (Appointment and AuditLog) so Atlas shows names instead of only ObjectId strings | Added `studentName` and `tutorName` string fields to `Appointment` schema; added `actorName` string field to `AuditLog` schema; updated `createAppointmentForSlot` in `studentController.js` to fetch tutor name at create time and save both `studentName` and `tutorName` alongside the ObjectId refs; added `name` to the JWT payload in all four sign locations (`adminController.js`, `tutorController.js`, `studentController.js`, `authRoutes.js`) so `req.user.name` is available everywhere; added `actorName: req.user.name` (or `tutor.name` / `student.name` where applicable) to all 11 `AuditLog.create()` calls across all three controllers | Verified by booking an appointment as Test Student and querying MongoDB directly — latest Appointment document showed `studentName: "Test Student", tutorName: "Test Tutor"`; latest AuditLog showed `actorName: "Test Student"` |
| Mar 21, 2026 | GitHub Copilot Chat | Asked to push latest changes to the Dillon branch | Staged all 12 modified tracked files (controllers, routes, models, middleware, views, server.js); committed as "Update controllers, routes, models, and dashboard changes" (commit `74ed481`); pushed to `origin/Dillon` | None |
| Mar 21, 2026 | GitHub Copilot Chat | Asked to add the three local cookie files to .gitignore so they stop appearing as untracked | Appended `admincookies.txt`, `cookies.txt`, `studentcookies.txt` to `.gitignore`; committed as "Ignore local cookie files" (commit `3d1f895`); pushed to `origin/Dillon` | None |
| Apr 9, 2026 | GitHub Copilot Chat | Asked to run the application | Read `package.json` to confirm `start`/`dev` scripts; attempted `npm run dev` — blocked by PowerShell execution-policy restriction on `npm.ps1`; switched to `node server.js` directly via background terminal; confirmed MongoDB connected and server listening on port 3001 | None |
| Apr 16, 2026 | GitHub Copilot Chat | Asked to fix admin dashboard audit log and notification log tables showing raw IDs instead of readable names | Updated `adminController.js` to map raw Mongoose docs to `actorDisplayName`, `targetLabel`, and `summary` for the audit log table; mapped `recipientDisplayName` and `summary` (from `providerResponse.messageId`) for the notification log table | None |
| Apr 16, 2026 | GitHub Copilot Chat | Asked to add Google Calendar API integration using full OAuth 2.0 (professor requires an API in the project) | Added `GOOGLE_CLIENT_ID`, `GOOGLE_CLIENT_SECRET`, `GOOGLE_CALLBACK_URL` to `.env`; installed `googleapis` npm package; extended `User` model with `googleRefreshToken`, `googleAccessToken`, `googleAccountEmail`; extended `Appointment` model with `googleCalendarEventId` and `googleCalendarSyncedAt`; created `Servers/middleware/googleCalendar.js` with helper functions (`getAuthUrl`, `getTokensFromCode`, `getGoogleAccountEmail`, `setCredentialsAndRefresh`, `addEventToCalendar`, `deleteEventFromCalendar`, `deleteMatchingEventsFromCalendar`); added `/auth/google` and `/auth/google/callback` routes to `authRoutes.js`; added `/student/addToGoogleCalendar/:appointmentId` and `/student/disconnectGoogleCalendar` routes to `studentRoutes.js`; mounted `authRoutes` at `/auth` in `server.js`; updated `studentController.showDashboard` to pass `googleConnected` and `googleAccountEmail`; updated `Views/Student/dashboard.ejs` with Connect banner, Disconnect button, Add to Calendar button, synced badge, and client-side JS | Fixed "Cannot GET /auth/google" by adding `app.use('/auth', authRoutes)` in `server.js` (was only mounted at `/api/auth`) |
| Apr 16, 2026 | GitHub Copilot Chat | Reported event deep-link opened "Could not find requested event" in Google Calendar; also needed connected account email captured reliably | Switched from event deep-links to calendar home URL (`calendar.google.com/calendar/u/0/r?authuser=...`) to avoid multi-account browser state conflicts; added `userinfo.email` OAuth scope to reliably capture the connected Google account email at callback time | None |
| Apr 16, 2026 | GitHub Copilot Chat | Asked to prevent duplicate Google Calendar events and auto-delete events when appointments are cancelled | Added `alreadySynced` check to `addToGoogleCalendar` route — returns early if `googleCalendarEventId` already set on the appointment; added synced badge to dashboard (replaces Add to Calendar button for already-synced appointments); added page reload after successful add; wired `cancelAppointment` in `studentController.js` to call `deleteEventFromCalendar` (by stored `eventId`) or `deleteMatchingEventsFromCalendar` fallback (by title+time match for older records without a stored event ID); cleaned up temporary debug scripts and `server.log` | None |
| Apr 16, 2026 | GitHub Copilot Chat | Asked to commit and push all Google Calendar changes to Dillon branch | Staged and committed 10 files as "Add Google Calendar OAuth sync and cancellation integration" (commit `fe02379`); pushed to `origin/Dillon`; 10 files changed, 745 insertions | None |
| Apr 20, 2026 | GitHub Copilot Chat | Asked to add AI_LOG entries and README AI Use section for all sessions (do not delete existing content, only add) | Read both `AI_LOG.md` and `README.md` in full to detect existing entries and follow established theme; appended Apr 9 and Apr 20 rows to the `AI_LOG.md` table; appended matching bullet points to the AI Use section in `README.md`; updated the session-dates summary line in README | None |
| Apr 20, 2026 | GitHub Copilot Chat | Asked to add AI_LOG entries and README AI Use section for the Apr 16 Google Calendar session (do not delete existing content, only add) | Read both `AI_LOG.md` and `README.md` in full to detect existing entries and follow established theme; inserted Apr 16 rows (audit log fix, Google Calendar OAuth, deep-link fix, duplicate prevention + cancellation sync, git push) into `AI_LOG.md` before the existing Apr 20 row; appended matching AI Use bullet points to `README.md`; updated session-dates summary line to include Apr 16 | None |
